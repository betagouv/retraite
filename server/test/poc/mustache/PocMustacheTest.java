package poc.mustache;

import org.junit.Test;

public class PocMustacheTest {

	@Test
	public void test_mustache() throws Exception {

		// final HashMap<String, Object> data = new HashMap<>();
		// data.put("name", "Jhon");

		// final StringWriter writer = new StringWriter();
		// final MustacheFactory mf = new DefaultMustacheFactory();
		// final Mustache mustache = mf.compile(new StringReader("Hello {{name}}, !"), "example");
		// mustache.execute(writer, data);
		// writer.flush();

		// assertThat(writer.toString()).isEqualTo("Hello Jhon, !");

	}
}
